"""
# Перевод в систему счисления больше 10:
def to_18(n):
    s = ''
    while n > 0:
        digit = n % 18
        if digit < 10:
            s = str(digit) + s
        else:
            s = chr(ord('A')+ digit - 10) + s
        n //= 18
    return s
#Объяснение:
def to_18(n):
    s = ''  # Создаем пустую строку, в которую будем собирать результат

    # Пока число n больше 0 (пока есть что переводить)
    while n > 0:

        # Шаг 1: Получаем последнюю "цифру" числа в 18-ричной системе
        # Операция % (остаток от деления) дает число от 0 до 17
        digit = n % 18

        # Шаг 2: Преобразуем полученную цифру в символ
        if digit < 10:
            # Если цифра от 0 до 9 - просто превращаем её в строку
            # Например: 5 -> '5'
            # Важно: добавляем В НАЧАЛО строки, потому что мы идем с конца числа
            s = str(digit) + s
        else:
            # Если цифра от 10 до 17 - превращаем в буквы A, B, C, ...
            # digit=10 -> 'A', digit=11 -> 'B', ... digit=17 -> 'H'
            # chr(ord('A') + (digit - 10)) работает так:
            #   ord('A') = 65 (код символа A)
            #   digit - 10 = 0 для 10, 1 для 11, и т.д.
            #   65 + 0 = 65 -> chr(65) = 'A'
            #   65 + 1 = 66 -> chr(66) = 'B'
            s = chr(ord('A') + digit - 10) + s

        # Шаг 3: Отсекаем последнюю цифру от числа n
        # Целочисленное деление на 18
        # Например: 123 // 18 = 6 (отбрасываем остаток 15)
        n //= 18
        # Возвращаемся к началу цикла с уменьшенным числом
    # Шаг 4: Возвращаем полученную строку
    return s

# Пример работы для числа 100:def to_18(n):
    s = ''  # Создаем пустую строку, в которую будем собирать результат
    # Пока число n больше 0 (пока есть что переводить)
    while n > 0:

        # Шаг 1: Получаем последнюю "цифру" числа в 18-ричной системе
        # Операция % (остаток от деления) дает число от 0 до 17
        digit = n % 18

        # Шаг 2: Преобразуем полученную цифру в символ
        if digit < 10:
            # Если цифра от 0 до 9 - просто превращаем её в строку
            # Например: 5 -> '5'
            # Важно: добавляем В НАЧАЛО строки, потому что мы идем с конца числа
            s = str(digit) + s
        else:
            # Если цифра от 10 до 17 - превращаем в буквы A, B, C, ...
            # digit=10 -> 'A', digit=11 -> 'B', ... digit=17 -> 'H'
            # chr(ord('A') + (digit - 10)) работает так:
            #   ord('A') = 65 (код символа A)
            #   digit - 10 = 0 для 10, 1 для 11, и т.д.
            #   65 + 0 = 65 -> chr(65) = 'A'
            #   65 + 1 = 66 -> chr(66) = 'B'
            s = chr(ord('A') + digit - 10) + s

        # Шаг 3: Отсекаем последнюю цифру от числа n
        # Целочисленное деление на 18
        # Например: 123 // 18 = 6 (отбрасываем остаток 15)
        n //= 18

        # Возвращаемся к началу цикла с уменьшенным числом

    # Шаг 4: Возвращаем полученную строку
    return s
# Пример работы для числа 100:
# 1-я итерация: digit = 100%18 = 10 -> 'A', n = 100//18 = 5
# 2-я итерация: digit = 5%18 = 5 -> '5', n = 5//18 = 0 (выход)
# Результат: '5' + 'A' = '5A'
# Проверка: 5*18^1 + 10*18^0 = 90 + 10 = 100 ✓
# 1-я итерация: digit = 100%18 = 10 -> 'A', n = 100//18 = 5
# 2-я итерация: digit = 5%18 = 5 -> '5', n = 5//18 = 0 (выход)
# Результат: '5' + 'A' = '5A'
# Проверка: 5*18^1 + 10*18^0 = 90 + 10 = 100 ✓
r = ''.join(sorted(r))[::-1] # Все цифры в записи сортируются по убыванию
da = int(r[-1]) + int(r[-2])
    r1 = to_5(da)
"""

'''
# Дубликация
for n in range(1, 1000):
    r = bin(n)[2:]
    if n % 2 == 0:
        r = r + '0' * r.count('0')   # дублируем каждый ноль
    else:
        r = r + '1' * r.count('1')   # дублируем каждую единицу
    R = int(r, 2)
'''
'''
map(функция, последовательность)
# Применяет функцию к каждому элементу и возвращает результат
r = "102"  # строка из цифр

# map(int, r) - превращает каждый символ в число
# "1" -> 1, "0" -> 0, "2" -> 2
результат_map = map(int, r)  # получаем объект map: [1, 0, 2]

sum(результат_map)  # 1 + 0 + 2 = 3
'''

'''
r = '123456789'
r = r[-1] + r[1:-1] + r[0] #  первая и последняя цифры меняются местами
print(r)
'''
'''
to_2 = [x for x in set(a) if a.count(x) == 2] #в строке ДВА числа повторяются дважды
if len(to_2) == 2
'''

# для системок счисления, букавы циферы
"""from string import printable
result = ''.join([i for i in printable])
print(result)"""

"""
# перебор делителей через корень, для больших чисел
x = 1_000_000
delit = []
for d in range(1, int(x ** 0.5) + 1):
    if x % d == 0:
        delit.append(d)
        delit.append(x // d)
print(set(delit))
"""
'''
# Функция для разложения любых чисел на произведение простых множителей:
def f(x):
    for d in range(2, int(x ** 0.5)  + 1):
        if x % d == 0:
            return [d] + f(x // d)
    return [x]
print(f(5))
print(f(16))
'''

'''
С помощью текстового редактора определите, сколько раз встречается 
сочетание букв «тон» или «Тон» только в начале слова, в тексте комедии 
А.С. Грибоедова «Горе от ума». Например, слово «стон» не подходит, а 
слово «тонкий» подходит. В ответе укажите только число.  
\b[Тт]он - с включенным регулярные выражения
'''

'''
Значение арифметического выражения 319 + 38 – х, где х – целое 
положительное число, превышающее 3000, записали в 3-ичной системе 
счисления. Определите наименьшее значение х, при котором количество 
нечётных цифр в 3-ичной записи числа, являющегося значением данного 
арифметического выражения, больше, чем чётных.  
В ответе запишите число в десятичной системе счисления.
d = 3**19 + 3**8
for x in range(3000, 100000):
  s = d - x
  k1 = 0
  k2 = 0
  while s > 0:
    if s % 3 == 1: # трочиная запись 012 только 1 нечетная
      k1 += 1
    else:
      k2 += 1
    s //= 3
  if k1 > k2:
    print(x)
    break
'''